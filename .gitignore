# Generated images from mflux capability
mflux_*.png

# Generated/test images in project root (do not commit)
/*.png
/*.jpg
/*.jpeg
/*.gif
/*.svg
/*.webp
/*.bmp

# Hermes agent working files (plans, opencode output, project boards)
.hermes/

# Python bytecode cache
__pycache__/
*.pyc

# Build artifacts
dist/
*.tar.gz

# Environment / secrets — never commit real .env
.env
.env.*
!.env.example

# Legacy SSN shell handler — superseded by the Python implementation
# (kept out of tracking on purpose; do not re-add)
nodes/handlers/ssn-capability-pages.sh
